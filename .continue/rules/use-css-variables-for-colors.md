---
globs: "**/*.js"
description: Apply this rule when editing or creating components that use
  colors. Replace hardcoded hex colors with appropriate CSS variables from the
  theme system.
alwaysApply: false
---

Always use CSS variables (var(--variable-name)) instead of hardcoded colors for text, backgrounds, borders, and gradients to ensure proper theme support